import { describe, test } from 'vitest'
// import { mount, VueWrapper } from '@vue/test-utils'

// import Index from '@/infrastructure/pages/index.vue'

// const mountIndex = (): VueWrapper => {
//   return mount(Index, {})
// }

describe('Pages Index.vue', () => {
  test('should mount the page', () => {
    // const wrapper = mountIndex()
    // expect(wrapper.vm).toBeDefined()
    // expect(wrapper.text()).toContain('The rest of the page')
  })

  test('should emit the event save with itens object', async () => {
    // const wrapper = mountIndex()
    // await wrapper.find('button').trigger('click')
    // console.log(wrapper.emitted().click)
    // expect(wrapper.emitted().save).toBeTruthy()
    // expect(wrapper.emitted().save.length).toBe(1)
    // expect(wrapper.emitted().save[0]).toEqual({})
  })
})
